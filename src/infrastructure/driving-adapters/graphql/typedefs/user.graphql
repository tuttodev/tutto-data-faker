type User { 
  id: ID!
  name: String!
  username: String!
  age: Int!
}

# Inputs
input UserCreateInput {
  name: String!
  username: String!
  age: Int!
}

input UserUpdateInput {
  id: ID!
  name: String
  username: String
  age: Int
}